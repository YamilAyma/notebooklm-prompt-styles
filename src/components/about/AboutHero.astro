---
---
<section class="about-hero">
  <div class="container">
    <h1 class="about-hero__title">Elevating AI-Powered Presentations</h1>
    <p class="about-hero__subtitle">
      NotebookLM Slide Styles is a curated digital library designed to transform 
      raw data into stunning visual narratives.
    </p>
  </div>
</section>

<style>
  .about-hero {
    padding: var(--space-4xl) var(--space-lg) var(--space-2xl);
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
  }
  .container {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }
  .about-hero__title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 1.1;
    color: var(--color-text);
    margin-bottom: var(--space-lg);
  }
  .about-hero__subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: var(--color-text-secondary);
    line-height: 1.5;
    max-width: 600px;
    margin: 0 auto;
  }
</style>
